sdocp('integrate.js.sdoc', 'Numerical integration in Caterwaul | Spencer Tipping\nLicensed under the terms of the MIT source code license\n\nIntroduction.\nThis library provides simple numerical integration using Simpson\'s rule. I\'ve also included an error function implementation for the sake of example.\n\ncaterwaul.js_all()(function () {\n  caterwaul.numerically_integrate(f, a, b, h) = (weighted_midpoints * 4 + weighted_endpoints * 2 + f(a) + f(b)) * h / 6\n                                                -where [weighted_midpoints = n[a + h/2, b - h/2, h] /[0][f(x) + x0] -seq,\n                                                        weighted_endpoints = n[a + h,   b,       h] /[0][f(x) + x0] -seq]})();\n');